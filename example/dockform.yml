docker:
  context: default
  identifier: demo

sops:
  age:
    key_file: ~/.config/sops/age/keys.txt
  recipients: 
    - age1ql3z7hjy54pw3hyww5ayyfg7zqgvc7w3j2elw8zmrj2kg5sfn9aqmcac8p

secrets:
  sops:
    - path: secrets.env
      format: dotenv

environment:
  files:
    - global.env
  inline:
    - GFOO=bar
    - GBAZ=qux

applications:
  website:
    root: website
    files:
      - docker-compose.yaml
    project:
      name: website
    environment:
      files:
        - vars.env
      inline:
        - FOO=bar
        - BAZ=qux
    secrets:
      sops:
        - path: secrets.env
          format: dotenv

volumes:
  demo-volume-1:

networks:
  demo-network: