identifier: ${DOCKFORM_RUN_ID}

contexts:
  default:
    volumes:
      df_e2e_${DOCKFORM_RUN_ID}_fsvol2: {}
    networks:
      df_e2e_${DOCKFORM_RUN_ID}_fsnet2: {}

stacks:
  default/app:
    root: .
    files:
      - docker-compose.yaml
    project:
      name: df_e2e_${DOCKFORM_RUN_ID}
