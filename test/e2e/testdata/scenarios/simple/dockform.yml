identifier: ${DOCKFORM_RUN_ID}

contexts:
  default:
    networks:
      df_e2e_${DOCKFORM_RUN_ID}_net: {}

stacks:
  default/app:
    root: .
    files:
      - docker-compose.yaml
    project:
      name: df_e2e_${DOCKFORM_RUN_ID}
