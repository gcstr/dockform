identifier: demo

contexts:
  default:
    volumes:
      demo-volume-1: {}
    networks:
      demo-network: {}

stacks:
  default/website:
    root: website
    files:
      - docker-compose.yaml
    project:
      name: website
    env-file:
      - vars.env
    environment:
      inline:
        - FOO=bar
        - BAZ=qux
