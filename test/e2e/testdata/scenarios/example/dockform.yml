identifier: demo

contexts:
  default:
    volumes:
      demo-volume-1: {}
    networks:
      demo-network: {}

stacks:
  default/website:
    root: website
    files:
      - docker-compose.yaml
    project:
      name: website
    environment:
      files:
        - vars.env
      inline:
        - FOO=bar
        - BAZ=qux
