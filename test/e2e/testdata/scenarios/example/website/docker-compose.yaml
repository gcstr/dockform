services:
  nginx:
    image: nginx:1.12
    ports:
      - "8000:80"
    environment:
      - PROJECT_LOCAL=${PROJECT_LOCAL}
      - PROJECT_GLOBAL=${PROJECT_GLOBAL}
      - FOO=${FOO}
      - BAZ=${BAZ}
      - GFOO=${GFOO}
      - GBAZ=${GBAZ}
      - SECRET_KEY=${SECRET_KEY}

